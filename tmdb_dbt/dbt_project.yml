name: tmdb_dbt
version: 1.0.0
config-version: 2

profile: tmdb_dbt

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  tmdb_dbt:
    staging:
      +schema: raw
      +materialized: view
    marts:
      +schema: analytics
      +materialized: table